import React from 'react';
import { SafeAreaView } from 'react-native';

import { generalStyles } from '../../../utils/styles';
import { IEndSequenceNarrativeScreenProps } from './EndSequenceNarrative.props';

export const EndSequenceNarrativeScreen: React.FC<IEndSequenceNarrativeScreenProps> =
  () => {
    return (
      <SafeAreaView style={generalStyles.whFlex}>
        {/* // TODO: Add screen code */}
      </SafeAreaView>
    );
  };
