import React from 'react';
import { SafeAreaView } from 'react-native';

import { generalStyles } from '../../../utils/styles';
import { IEndSequenceDualElixirScreenProps } from './EndSequenceDualElixir.props';

export const EndSequenceDualElixirScreen: React.FC<IEndSequenceDualElixirScreenProps> =
  () => {
    return (
      <SafeAreaView style={generalStyles.whFlex}>
        {/* // TODO: Add screen code */}
      </SafeAreaView>
    );
  };
