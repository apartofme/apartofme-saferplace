import React from 'react';
import { SafeAreaView } from 'react-native';

import { generalStyles } from '../../../utils/styles';
import { IEndSequenceShakeScreenProps } from './EndSequenceShake.props';

export const EndSequenceShakeScreen: React.FC<IEndSequenceShakeScreenProps> =
  () => {
    return (
      <SafeAreaView style={generalStyles.whFlex}>
        {/* // TODO: Add screen code */}
      </SafeAreaView>
    );
  };
