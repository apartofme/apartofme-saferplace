import React from 'react';
import { SafeAreaView } from 'react-native';

import { generalStyles } from '../../../utils/styles';
import { IEndSequenceFinalScreenProps } from './EndSequenceFinal.props';

export const EndSequenceFinalScreen: React.FC<IEndSequenceFinalScreenProps> =
  () => {
    return (
      <SafeAreaView style={generalStyles.whFlex}>
        {/* // TODO: Add screen code */}
      </SafeAreaView>
    );
  };
